---
"@runfusion/fusion": patch
---

summary: Route cursor-cli planning/execution through the Cursor runtime instead of the pi registry.
category: fix
dev: ISS-CLI-005 — createResolvedAgentSession auto-derives runtime hint `cursor` for cursor-cli; CursorRuntimeAdapter invokes `cursor-agent -p` with discovery validation.
