---
"@runfusion/fusion": patch
---

summary: Stop browser credential autofill from hijacking Settings non-identity fields.
category: fix
dev: ISS-UI-001 expanded — search filter plus Cursor/Grok/OMP binary paths (read-only until focus) and API-key Replace-only password inputs with new-password autocomplete.
